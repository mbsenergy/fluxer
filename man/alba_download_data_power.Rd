% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/other_apis.R
\name{alba_download_data_power}
\alias{alba_download_data_power}
\title{Download and Process ALBA Data}
\usage{
alba_download_data_power(
  from_date,
  to_date,
  type,
  output_dir,
  username,
  password,
  raw = FALSE
)
}
\arguments{
\item{from_date}{A \code{Date} object indicating the start date for data processing (not directly used in this code but can be added for filtering).}

\item{to_date}{A \code{Date} object indicating the end date for data processing (not directly used in this code but can be added for filtering).}

\item{output_dir}{A string specifying the directory where the downloaded file will be saved.}

\item{username}{A string containing the username for authentication.}

\item{password}{A string containing the password for authentication.}
}
\value{
A list containing:
\item{sheet_data_list}{A list of \code{data.table} objects, one for each sheet in the Excel file.}
\item{combined_data}{A combined \code{data.table} containing all data from the Excel sheets.}
}
\description{
This function logs into the ALBA energy platform, downloads a data file, and processes its content from multiple Excel sheets.
}
\details{
The function performs the following steps:
\enumerate{
\item Logs into the ALBA energy platform using provided credentials.
\item Downloads the Excel file containing data.
\item Processes each sheet of the Excel file:
\itemize{
\item Reads the data.
\item Transforms it into long format.
\item Removes rows with missing values.
\item Adds metadata about the sheet name.
}
\item Combines data from all sheets into a single \code{data.table} for easier analysis.
}
}
\examples{
\dontrun{
# Define parameters
from_date <- as.Date("2023-01-01")
to_date <- as.Date("2023-12-31")
output_dir <- "data"
username <- "your_username"
password <- "your_password"

# Call the function
result <- alba_download_data(from_date, to_date, output_dir, username, password)

# Access individual sheet data
sheet_data_list <- result$sheet_data_list

# Access combined data
combined_data <- result$combined_data
}
}
